# Sportz

Monorepo: Nx at the root, with the backend in `server/` and the UI in `client/`.

Run both apps:

```bash
npm run dev
```

Run just the API:

```bash
npm run dev:server
```

Run just the UI:

```bash
npm run dev:client
```
